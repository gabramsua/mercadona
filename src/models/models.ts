export interface User {
    nombre: string;
    apellidos: string;
    telefono: string;
    password: string;
}

export interface Tornillo {
    id: string;
    nombre: string;
    precio: number;
    formato: string;
    marca: string;
}